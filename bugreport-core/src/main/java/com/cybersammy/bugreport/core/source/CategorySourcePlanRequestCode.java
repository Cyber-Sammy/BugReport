package com.cybersammy.bugreport.core.source;

/** Stable rejection reason for a category source-plan request. */
public enum CategorySourcePlanRequestCode {
    PROVIDER_NOT_REGISTERED,
    PROVIDER_DISABLED,
    CATEGORY_NOT_DECLARED
}
