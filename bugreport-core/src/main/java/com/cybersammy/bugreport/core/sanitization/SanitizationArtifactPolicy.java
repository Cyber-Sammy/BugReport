package com.cybersammy.bugreport.core.sanitization;

/** Source-aware policy used when interpreting potentially sensitive patterns. */
public enum SanitizationArtifactPolicy {
    LOG,
    CONFIGURATION
}
